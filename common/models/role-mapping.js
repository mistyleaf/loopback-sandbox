/**
 * Extends Built-in RoleMapping model.
 *
 * **Default ACLs**
 *
 *  - DENY EVERYONE `*`
 *
 * @property {String} id Generated ID.
 * @property {String} principalType One of USER, APP or ROLE.
 * @property {String} principalId ID of principal type.
 * @property {String} roleId Role that the principal belongs to.
 *
 * @class RoleMapping
 * @inherits RoleMapping (loopback built-in model)
 */

/**
 * RoleMapping
 * @param {String} RoleMapping Object
 * @returns {void}
 */
module.exports = function(RoleMapping) { // eslint-disable-line func-names

};
